# Consumer R8/ProGuard rules shipped inside the AAR.
#
# The SDK's public API is annotated with @androidx.annotation.Keep, and the
# bundled dependencies (kotlinx-serialization, Retrofit, OkHttp) ship their own
# consumer rules, so no additional rules are required here.
